{
  "name": "kairos-journaling-app",
  "version": "1.0.0",
  "description": "A web-based journaling application with AI features, designed for self-reflection and growth.",
  "main": "index.html",
  "scripts": {
    "start": "python -m http.server 8080 || python3 -m http.server 8080 || npx http-server -p 8080",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.7",
    "firebase": "^10.12.2",
    "firebase-admin": "^11.10.1"
  }
}
